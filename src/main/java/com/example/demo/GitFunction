# КРОК 1: Перевірка, де ми зараз
# Показує список гілок, зірочка (*) стоїть біля активної.
git branch

# КРОК 2: Створення нової гілки та перехід на неї
# Прапорець -b означає "створити і одразу перейти".
# Замість "new-feature" придумайте свою назву (напр. "add-login").
git checkout -b new-feature

# --- ТУТ ВИ ПИШЕТЕ КОД У СВОЇХ ФАЙЛАХ ---

# КРОК 3: Збереження змін у цій гілці
# Додаємо файли до індексу
git add .
# Фіксуємо зміни (вони збережуться ТІЛЬКИ в гілці new-feature)
git commit -m "Опис того, що я зробив"

# КРОК 4: Повернення на головну гілку (Корінний проект)
# Увага! Ваші нові зміни зникнуть з файлів, бо вони залишилися в іншій гілці.
# Це нормально. Ви повернулися у "минуле" (стабільну версію).
git checkout main

# КРОК 5: Вливання змін (Якщо робота завершена)
# Ви знаходитесь на main і хочете забрати зміни з new-feature сюди.
git merge new-feature

# КРОК 6: Видалення гілки (Якщо вона вже не потрібна після злиття)
git branch -d new-feature


git status
git add .
git commit -m
git push